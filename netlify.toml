[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Variables de entorno (descomenta y configura según necesites)
# [context.production.environment]
#   VITE_API_URL = "https://api.tu-dominio.com"

# [context.deploy-preview.environment]
#   VITE_API_URL = "https://api-staging.tu-dominio.com"


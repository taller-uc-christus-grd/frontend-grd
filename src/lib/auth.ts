import type {Role,User} from '@/types'; const K='grd_user'; export const getStoredUser=()=>{const r=localStorage.getItem(K); return r?JSON.parse(r) as User:null}; export const storeUser=(u:User|null)=>{u?localStorage.setItem(K,JSON.stringify(u)):localStorage.removeItem(K)}; export const hasRole=(u:User|null,a:Role[])=>!!u&&a.some(r=>r.toLowerCase()===u.role.toLowerCase());

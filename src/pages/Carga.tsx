import { useState } from 'react'; export default function Carga(){ const [file,setFile]=useState<File|null>(null); const [status,setStatus]=useState(''); async function upload(){ if(!file) return; setStatus('Subiendo...'); await new Promise(r=>setTimeout(r,700)); setStatus('Validado: 120 filas, 3 errores, 117 válidas.'); } return(<main className='max-w-3xl mx-auto px-4 py-10'><h1 className='text-xl font-semibold'>Carga de archivo maestro</h1><div className='bg-white rounded-xl p-6 border mt-4'><input type='file' accept='.csv,.xls,.xlsx' onChange={e=>setFile(e.target.files?.[0]||null)} /><button onClick={upload} className='ml-3 px-3 py-2 rounded-lg bg-indigo-600 text-white'>Subir</button><p className='mt-3 text-sm text-slate-600'>{status}</p></div></main>); }
